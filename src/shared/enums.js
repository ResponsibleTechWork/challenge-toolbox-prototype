const Scope = Object.freeze({
    Board: 'board',
    Member: 'member',
    Card: 'card',
});

const Type = Object.freeze({
    Challenge: 'challenge',
    Celebtrate: 'celebtrate'
})

const enums = {
    Scope,
    Type
};

export default enums;